/**
 * App configuration
 * Add leagues to this array to include them in game listings
 */
export const config = {
  /**
   * Leagues to display in game listings.
   * Available: 'NBA', 'NCAAB', 'NFL', 'MLB', 'NHL', etc.
   */
  enabledLeagues: ['NBA'] as string[],
};

